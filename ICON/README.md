Place your custom icon files in this folder.

Expected files:
- `WIN.ico` : used by the app window at runtime.
- `DESK.ico` : reserved for packaged desktop app / EXE builds.

The Python app will automatically use `WIN.ico` when the file exists.
